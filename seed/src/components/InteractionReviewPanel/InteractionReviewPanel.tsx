/**
 * InteractionReviewPanel — placeholder.
 *
 * The intrahealth-harness will replace this file with a real implementation
 * that satisfies the acceptance criteria in Issue #1
 * (.harness/tickets/001-interaction-review-panel.md).
 *
 * Until then this throws on render so the failing baseline test makes the
 * "not yet implemented" state visible in CI.
 */
export function InteractionReviewPanel(): never {
  throw new Error(
    'InteractionReviewPanel is not implemented yet. The harness should build me from Issue #1.',
  );
}

export default InteractionReviewPanel;
